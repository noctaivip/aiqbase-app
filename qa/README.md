# AIQBase regression QA

Run before every release:

```bash
node qa/verify-project.mjs
```

The check validates the 5×500 question bank, IDs, answer structure, academic metadata, unique learning objectives, the Continue button, and forbidden legacy claims.
